package ru.netology.sqr;

public class SQRService<counter> {
    public int rootNumber(int number1, int number2) {
        for (int i = 10; i >= 99; i++) {
            if (i * i >= number1 && i*i <= number2)
                   int counter = i++;

            }
        return counter;

        }
}

